Build a persistent job queue as a Python library in this directory.

A job has a payload (arbitrary JSON) and moves through states. The library must
support enqueueing a job, claiming the next available job, marking a claimed job
done, and marking a claimed job failed.

Queue contents must survive process restart.

Include tests and a short README describing the public API. Use only the Python
standard library.
