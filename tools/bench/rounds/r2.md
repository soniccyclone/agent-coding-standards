Extend the job queue with retry handling.

A job that fails should be retried up to a configurable maximum number of
attempts, with exponential backoff between attempts. A job that exhausts its
attempts moves to a dead-letter state and stays there for inspection.

No job may be lost at any point, including if the process exits between a
failure and the scheduling of its retry.

Update the tests and the README.
